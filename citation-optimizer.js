/* Publify — Citation Optimizer (CitationOptimizer.html?project=<id>).
 * Drives the citation-optimizer edge function (run → analyze_paper loop → finalize → get) and renders the
 * project-level strategy + the ranked top-10 cards: intent mix, influential ratio, contributions, and the
 * real citation sentences. Plain DOM (no React). */
(function () {
  'use strict';
  var BE = window.PR_BACKEND, sb = BE && BE.sb, CFG = window.PR_CONFIG || {};
  var root = document.getElementById('root');
  var INTENTS = ['method', 'result', 'background', 'data', 'contrast'];
  var INTENT_LBL = { method: 'Method', result: 'Result', background: 'Background', data: 'Data', contrast: 'Contrast' };

  function esc(s) { return String(s == null ? '' : s).replace(/[&<>"]/g, function (x) { return { '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;' }[x]; }); }
  function projId() { try { return new URLSearchParams(location.search).get('project'); } catch (e) { return null; } }
  function sleep(ms) { return new Promise(function (r) { setTimeout(r, ms); }); }
  function bareDoi(d) { return String(d || '').trim().replace(/^https?:\/\/(dx\.)?doi\.org\//i, '').replace(/^doi:/i, ''); }
  function mdSafe(md) { try { return DOMPurify.sanitize(marked.parse(String(md || ''))); } catch (e) { return esc(md || ''); } }
  function downloadText(name, text) { var b = new Blob([text], { type: 'text/markdown' }); var a = document.createElement('a'); a.href = URL.createObjectURL(b); a.download = name; a.click(); setTimeout(function () { URL.revokeObjectURL(a.href); }, 1000); }

  function toast(msg, ok) { var t = document.createElement('div'); t.className = 'co-toast' + (ok === false ? ' err' : ''); t.textContent = msg; document.body.appendChild(t); requestAnimationFrame(function () { t.classList.add('show'); }); setTimeout(function () { t.classList.remove('show'); setTimeout(function () { if (t.parentNode) t.parentNode.removeChild(t); }, 260); }, 2600); }

  // ---- P3: push the strategy into the project's documents (surfaces in Protocol files / attachable in Writing) ----
  function mdCell(s) { return String(s == null ? '' : s).replace(/\s*\n+\s*/g, ' ').replace(/\|/g, '\\|').trim(); }
  function topIntentOf(mix) { var best = '', n = 0, total = 0; INTENTS.forEach(function (k) { total += (mix && mix[k]) || 0; }); INTENTS.forEach(function (k) { if ((mix && mix[k] || 0) > n) { n = mix[k]; best = k; } }); return total ? { k: best, pct: Math.round(100 * n / total) } : null; }
  function briefTable() {
    var rows = (S.insights || []).slice().sort(function (a, b) { return (a.rank || 0) - (b.rank || 0); }).map(function (x) {
      var ti = topIntentOf(x.intent_mix), topC = (x.contributions && x.contributions[0] && x.contributions[0].label) || '';
      var mostFor = ti ? (INTENT_LBL[ti.k] + ' ' + ti.pct + '%' + (topC ? ' · ' + topC : '')) : (x.summary && /unavailable/i.test(x.summary) ? 'contexts unavailable' : '—');
      return '| ' + (x.rank || '') + ' | ' + mdCell((x.title || 'Untitled') + (x.year ? ' (' + x.year + ')' : '')) + ' | ' + (x.cited_by != null ? (+x.cited_by).toLocaleString() : '—') + ' | ' + mdCell(mostFor) + ' | ' + mdCell(x.summary || '') + ' |';
    }).join('\n');
    return '| # | Paper | Citations | Most cited for | How the field cites it |\n|---|---|---|---|---|\n' + rows;
  }
  function buildBrief(target) {
    var rep = S.report || {};
    var head = target === 'protocol'
      ? '# Citation landscape for the protocol\n\nFor each planned claim, this shows which key paper to cite and *why the field cites it* — so the related-work plan is a positioned argument, not a list.\n'
      : '# Citation positioning for the write-up\n\nIn Related Work, cite each paper for the contribution the field actually attributes to it, and steer your novelty toward the uncrowded angles.\n';
    var prov = '\n\n---\n*Generated by the Publify Citation Optimizer' + (rep.stats && rep.stats.contexts ? ' from ' + rep.stats.contexts + ' citation contexts (Semantic Scholar)' : '') + '. Verify every suggested citation against the source before use.*\n';
    return head + '\n' + (rep.strategy || '') + '\n\n## How the field cites your key papers\n\n' + briefTable() + prov;
  }
  // the write-up framing directive derived from the top papers (this becomes a task instruction + step notes)
  function directiveText() {
    var items = (S.insights || []).filter(function (x) { return x.summary && x.s2_id && x.intent_mix; }).slice(0, 5).map(function (x) {
      var ti = topIntentOf(x.intent_mix), c = (x.contributions && x.contributions[0] && x.contributions[0].label) || '';
      return '• ' + (x.title || 'paper') + ' — cite for ' + (c || (ti ? INTENT_LBL[ti.k].toLowerCase() : 'its contribution')) + (ti ? ' (' + INTENT_LBL[ti.k].toLowerCase() + ', ' + ti.pct + '% of its citations)' : '');
    });
    return 'Related-Work citation framing (from the Citation Optimizer) — cite each key paper for what the field actually cites it for:\n' + items.join('\n') + '\n\nSee the full brief: protocol/citation-strategy.md';
  }
  function saveBrief(target) {
    var path = target === 'protocol' ? 'protocol/citation-strategy.md' : 'writing/citation-strategy.md';
    var content = buildBrief(target), uid = (BE.user && BE.user.id) || null;
    return sb.from('research_files').upsert({ project_id: projId(), path: path, content: content, mime: 'text/markdown', size: content.length, source: 'ai', created_by: uid, updated_by: uid, updated_at: new Date().toISOString() }, { onConflict: 'project_id,path' });
  }
  var WRITEUP_RE = /related work|write-?up|writing|discussion|manuscript|\bpaper\b|introduction/i;
  // build a concrete plan of exactly what "Use in ..." will change, then show it for confirmation
  function planUse(target) {
    if (!(S.report && S.report.strategy)) { toast('Run the analysis first.', false); return; }
    if (target === 'writing') { showModal({ target: 'writing', doc: 'writing/citation-strategy.md', steps: [], newTask: null, protocol: null }); return; }
    // protocol: fetch the current protocol + its steps so we can show the real tasks affected
    toast('Reading your protocol…');
    sb.from('research_protocols').select('id,title').eq('project_id', projId()).neq('status', 'archived').order('created_at', { ascending: false }).limit(1).maybeSingle().then(function (pr) {
      var prot = pr && pr.data;
      if (!prot) { showModal({ target: 'protocol', doc: 'protocol/citation-strategy.md', steps: [], newTask: null, protocol: null }); return; }
      sb.from('research_protocol_steps').select('id,ord,title,kind').eq('protocol_id', prot.id).order('ord').then(function (sr) {
        var steps = (sr && sr.data) || [];
        var targets = steps.filter(function (s) { return s.kind === 'writeup' || WRITEUP_RE.test(s.title || ''); });
        showModal({ target: 'protocol', doc: 'protocol/citation-strategy.md', protocol: prot, allSteps: steps, steps: targets, directive: directiveText(),
          newTask: { title: 'Frame Related Work with the citation strategy', kind: 'writeup' } });
      });
    });
  }
  function applyPlan(plan) {
    var applyBtn = document.getElementById('mApply'); if (applyBtn) { applyBtn.disabled = true; applyBtn.textContent = 'Applying…'; }
    var restore = function (lbl) { if (applyBtn) { applyBtn.disabled = false; applyBtn.textContent = lbl; } };
    var uid = (BE.user && BE.user.id) || null;
    var jobs = [saveBrief(plan.target)];
    if (plan.target === 'protocol' && plan.protocol) {
      var maxOrd = (plan.allSteps || []).reduce(function (m, x) { return Math.max(m, x.ord || 0); }, 0);
      // tag the step + notes with origin so the Protocol panel can mark them
      jobs.push(sb.from('research_protocol_steps').insert({ protocol_id: plan.protocol.id, ord: maxOrd + 1, title: plan.newTask.title, kind: 'writeup', spec: { instruction: plan.directive, origin: 'citation-optimizer' }, depends_on: [], needs_approval: true }));
      (plan.steps || []).forEach(function (s) {
        jobs.push(sb.from('research_protocol_notes').insert({ project_id: projId(), protocol_id: plan.protocol.id, step_id: s.id, author_id: uid, author_name: 'Citation Optimizer', kind: 'dir', body: plan.directive }));
      });
    }
    Promise.all(jobs).then(function (rs) {
      var err = rs.find(function (r) { return r && r.error; });
      if (err) { restore(plan.protocol ? 'Apply to Protocol' : 'Save document'); toast('Some changes failed: ' + err.error.message, false); return; }
      showSuccess(plan);
    }, function (e) { restore(plan.protocol ? 'Apply to Protocol' : 'Save document'); toast('Could not apply: ' + ((e && e.message) || e), false); });
  }
  function showSuccess(plan) {
    if (!modalEl) { toast('✓ Applied.'); return; }
    var pid = projId(), isProt = plan.target === 'protocol' && plan.protocol;
    var added = isProt
      ? '<ul class="cm-done"><li><b>➕ 1 new task</b> — ' + esc(plan.newTask.title) + '</li>' + (plan.steps.length ? '<li><b>📌 ' + plan.steps.length + ' instruction' + (plan.steps.length === 1 ? '' : 's') + '</b> — added to your write-up task' + (plan.steps.length === 1 ? '' : 's') + '</li>' : '') + '<li><b>📄 1 document</b> — ' + esc(plan.doc) + '</li></ul>'
      : '<ul class="cm-done"><li><b>📄 1 document</b> — ' + esc(plan.doc) + '</li></ul>';
    var openBtn = isProt
      ? '<a class="btn pri" href="Research.html?project=' + encodeURIComponent(pid) + '&step=1">Open Protocol →</a>'
      : '<a class="btn pri" href="Research.html?project=' + encodeURIComponent(pid) + '">Open Research →</a>';
    modalEl.querySelector('.co-modal').innerHTML =
      '<div class="cm-head"><b>Applied to your ' + (plan.target === 'protocol' ? 'Protocol' : 'Writing') + '</b><button class="cm-x" id="mX2" aria-label="Close">✕</button></div>'
      + '<div class="cm-body"><div class="cm-success"><div class="cm-check">✓</div><div style="flex:1"><b>Done — here is what was added:</b>' + added
      + (isProt ? '<p class="cm-hint">In the <b>Protocol</b> tab these are marked with a <span class="cm-badge">🔗 Citation Optimizer</span> tag so you can spot them.</p>' : '') + '</div></div></div>'
      + '<div class="cm-foot"><button class="btn" id="mDone">Done</button>' + openBtn + '</div>';
    document.getElementById('mX2').onclick = closeModal;
    document.getElementById('mDone').onclick = closeModal;
  }

  // ---- preview modal: shows EXACTLY what "Use in ..." will add, before it applies ----
  var modalEl = null;
  function closeModal() { if (modalEl) { modalEl.remove(); modalEl = null; } }
  function cmItem(icon, title, sub, detail) { return '<div class="cm-item"><div class="cm-ic">' + icon + '</div><div class="cm-txt"><b>' + title + '</b>' + (sub ? '<div class="cm-sub">' + sub + '</div>' : '') + (detail ? '<div class="cm-det">' + detail + '</div>' : '') + '</div></div>'; }
  function showModal(plan) {
    closeModal();
    var body = '', footer = '', title = plan.target === 'protocol' ? 'Add the citation strategy to your Protocol' : 'Add the citation strategy to your Writing';
    if (plan.target === 'protocol' && plan.protocol) {
      body = '<p class="cm-lede">Here is exactly what will be added to <b>' + esc(plan.protocol.title || 'your protocol') + '</b>. Review, then apply — nothing changes until you do.</p>'
        + cmItem('➕', 'A new task', esc(plan.newTask.title) + ' <span class="cm-tag">needs approval</span>', 'Its instruction:<div class="cm-pre">' + esc(plan.directive) + '</div>')
        + (plan.steps.length
          ? cmItem('📌', 'An instruction on ' + plan.steps.length + ' existing task' + (plan.steps.length === 1 ? '' : 's'), plan.steps.map(function (s) { return esc('#' + s.ord + ' · ' + s.title); }).join(' &nbsp;·&nbsp; '), 'The same directive is attached as a note (kind: directive) to each:<div class="cm-pre">' + esc(plan.directive) + '</div>')
          : cmItem('📌', 'Instructions on existing tasks', '<span class="cm-muted">No Related-Work / write-up task found — the new task above carries the instruction instead.</span>', ''))
        + cmItem('📄', 'A document', esc(plan.doc), 'The full strategy + a table of how the field cites each key paper. Shows up in your Protocol files.');
      footer = '<button class="btn" id="mCancel">Cancel</button><button class="btn pri" id="mApply">Apply to Protocol</button>';
    } else if (plan.target === 'protocol') {
      body = '<div class="cm-warn">No protocol exists yet, so no tasks or instructions can be added. Generate a protocol first (the <b>Protocol</b> tab), then run this again — it will add a task and attach citation instructions to your write-up tasks.</div>'
        + cmItem('📄', 'A document', esc(plan.doc), 'For now the strategy + citation table will be saved to your Protocol files.');
      footer = '<button class="btn" id="mCancel">Cancel</button><button class="btn pri" id="mApply">Save document</button>';
    } else {
      body = '<p class="cm-lede">The Writing step composes a LaTeX draft, so this saves a <b>positioning brief</b> you can open and pull from while drafting Related Work.</p>'
        + cmItem('📄', 'A document', esc(plan.doc), 'Strategy + a table of what each key paper is cited for, saved to your Writing documents.');
      footer = '<button class="btn" id="mCancel">Cancel</button><button class="btn pri" id="mApply">Save document</button>';
    }
    modalEl = document.createElement('div'); modalEl.className = 'co-modal-scrim';
    modalEl.innerHTML = '<div class="co-modal" role="dialog" aria-modal="true"><div class="cm-head"><b>' + esc(title) + '</b><button class="cm-x" id="mX" aria-label="Close">✕</button></div><div class="cm-body">' + body + '</div><div class="cm-foot">' + footer + '</div></div>';
    document.body.appendChild(modalEl);
    modalEl.addEventListener('click', function (e) { if (e.target === modalEl) closeModal(); });
    document.getElementById('mX').onclick = closeModal;
    document.getElementById('mCancel').onclick = closeModal;
    document.getElementById('mApply').onclick = function () { applyPlan(plan); };
  }

  function callFn(fn, body) {
    return sb.auth.getSession().then(function (s) {
      var token = (s && s.data && s.data.session && s.data.session.access_token) || CFG.supabaseAnonKey;
      return fetch(CFG.supabaseUrl + '/functions/v1/' + fn, { method: 'POST', headers: { 'Content-Type': 'application/json', 'apikey': CFG.supabaseAnonKey, 'Authorization': 'Bearer ' + token }, body: JSON.stringify(body) }).then(function (r) { return r.json().catch(function () { return {}; }); });
    });
  }
  function co(body) { return callFn('citation-optimizer', body); }

  var S = { report: null, insights: [], busy: null, err: null };

  function load() {
    return co({ action: 'get', project_id: projId() }).then(function (r) {
      if (r && r.ok) { S.report = r.report || null; S.insights = r.insights || []; }
      render();
    });
  }

  // ---- the driven pipeline: run (retry on 503) → analyze each paper (retry on transient) → finalize ----
  var running = false;
  function runAnalysis() {
    if (running) return; running = true; S.err = null;
    setBusy('Selecting your top-cited included papers…', 0, 1);
    (async function () {
      try {
        var r = null;
        for (var a = 0; a < 4; a++) {
          r = await co({ action: 'run', project_id: projId() });
          if (r && r.report_id) break;
          if (r && r.error === 'no_included') { fail('No included papers with a citation count yet. In Literature, mark some high-impact papers as Include, then run this.'); return; }
          if (r && r.retryable) { setBusy('Semantic Scholar is busy — retrying…', 0, 1); await sleep(5000); continue; }
          fail((r && (r.message || r.error)) || 'Could not start the analysis.'); return;
        }
        if (!r || !r.report_id) { fail('Semantic Scholar is rate-limiting right now. Try again in a minute.'); return; }
        var reportId = r.report_id, papers = (r.papers || []);
        var pending = papers.filter(function (p) { return !p.done; });
        for (var i = 0; i < pending.length; i++) {
          var p = pending[i];
          setBusy('Reading citation contexts — ' + (p.title || 'paper').slice(0, 46) + '…', i, pending.length);
          for (var att = 0; att < 3; att++) {
            var ar = await co({ action: 'analyze_paper', report_id: reportId, insight_id: p.id, attempt: att });
            if (ar && ar.retryable) { setBusy('Rate-limited — waiting to retry this paper…', i, pending.length); await sleep(3500); continue; }
            break; // ok / skipped / gave_up → move on
          }
        }
        setBusy('Writing the citation strategy…', pending.length, pending.length);
        await co({ action: 'finalize', report_id: reportId });
        S.busy = null; running = false;
        await load();
      } catch (e) { fail('Something went wrong: ' + ((e && e.message) || e)); }
    })();
  }
  function fail(msg) { running = false; S.busy = null; S.err = msg; render(); }
  function setBusy(msg, done, total) { S.busy = { msg: msg, done: done, total: total }; render(); }

  // ---- render ----
  function nd() { return !!(window.PRDesign && window.PRDesign.isNew()); }
  function shell(contentHTML) {
    root.innerHTML = '<div class="topbar"><div class="topbar-in">'
      + '<a class="brand" href="Research.html?project=' + esc(projId() || '') + '"><span class="mk"><i></i></span><span>Publify<small>Citation Optimizer</small></span></a>'
      + '<span class="spring"></span>'
      + '<button class="btn ic" id="co-dark" title="Toggle dark mode" aria-label="Toggle dark mode">◐</button>'
      + '<button class="btn ic' + (nd() ? ' on' : '') + '" id="co-design" title="Toggle the new design (beta)" aria-label="Toggle the new design">✨</button>'
      + '<a class="btn" href="Research.html?project=' + esc(projId() || '') + '">← Research</a></div></div>'
      + '<div class="wrap" id="wrap">' + contentHTML + '</div>';
    var cd = document.getElementById('co-dark'); if (cd) cd.onclick = function () { if (window.PRTheme) window.PRTheme.toggle(); };
    var cg = document.getElementById('co-design'); if (cg) cg.onclick = function () { if (window.PRDesign) { window.PRDesign.toggle(); cg.classList.toggle('on', window.PRDesign.isNew()); } };
  }

  function intentMix(mix) {
    mix = mix || {}; var total = 0; INTENTS.forEach(function (k) { total += (mix[k] || 0); });
    if (!total) return '';
    var segs = INTENTS.filter(function (k) { return mix[k]; }).map(function (k) { return '<i class="' + k + '" style="width:' + (100 * mix[k] / total).toFixed(1) + '%"></i>'; }).join('');
    var keys = INTENTS.filter(function (k) { return mix[k]; }).map(function (k) { return '<span class="k"><i style="background:var(--' + (k === 'background' ? 'backg' : k) + ')"></i>' + INTENT_LBL[k] + ' ' + Math.round(100 * mix[k] / total) + '%</span>'; }).join('');
    return '<div class="mixwrap"><div class="mixbar">' + segs + '</div><div class="mixkey">' + keys + '</div></div>';
  }

  function card(x, i) {
    var mix = x.intent_mix, hasMix = mix && INTENTS.some(function (k) { return mix[k]; });
    var contribs = (x.contributions || []).filter(function (c) { return c && c.label; }).slice(0, 6)
      .map(function (c) { return '<span class="contrib">' + esc(c.label) + (c.count ? '<span class="pc">×' + (+c.count) + '</span>' : '') + '</span>'; }).join('');
    var ctxs = (x.contexts || []).filter(function (c) { return c && c.sentence; });
    var ctxHtml = ctxs.map(function (c) {
      var ic = c.intent && INTENT_LBL[c.intent] ? '<span class="chip ' + c.intent + '">' + INTENT_LBL[c.intent] + '</span>' : '';
      var star = c.influential ? ' <span class="chip influ">Influential</span>' : '';
      return '<div class="ctx"><div class="sent">“' + esc(c.sentence) + '”</div><div class="foot"><span class="who">' + esc(c.citing_title || 'Citing paper') + '</span>' + (c.year ? ' · ' + c.year : '') + ' ' + ic + star + '</div></div>';
    }).join('');
    var citeStr = x.cited_by != null ? '<div class="cites"><div class="big num">' + (+x.cited_by).toLocaleString() + '</div><div class="lbl">citations</div></div>' : '<div class="cites"></div>';
    return '<div class="pcard"><div class="row1"><div class="rank">' + (x.rank || i + 1) + '</div>'
      + '<div class="pmeta"><b>' + esc(x.title || 'Untitled') + '</b><div class="venue">' + esc([x.venue, x.year].filter(Boolean).join(' · ')) + (x.influential ? ' · ' + x.influential + ' influential' : '') + '</div></div>'
      + citeStr + '</div>'
      + (hasMix ? intentMix(mix) : '')
      + (contribs ? '<div class="citedfor"><div class="h">Cited for</div><div class="contribs">' + contribs + '</div></div>' : '')
      + (x.summary ? '<div class="summary"><b>Why it\'s cited:</b> ' + esc(x.summary) + '</div>' : '')
      + (ctxHtml ? '<details class="ctxs"><summary>' + ctxs.length + ' citation sentence' + (ctxs.length === 1 ? '' : 's') + '</summary><div class="ctxlist">' + ctxHtml + '</div></details>' : '')
      + '</div>';
  }

  function render() {
    // busy / progress
    if (S.busy) {
      var b = S.busy, frac = b.total ? Math.round(100 * b.done / b.total) : 0;
      shell('<div class="runbar"><span class="spin"></span><span class="msg">' + esc(b.msg) + '</span><span class="pbar"><i style="width:' + frac + '%"></i></span></div>'
        + '<div class="empty"><p>Analyzing how the field cites your key papers. You can leave this open — it runs a paper at a time so Semantic Scholar isn\'t overloaded.</p></div>');
      return;
    }
    var rep = S.report, done = rep && rep.status === 'done';
    var runBtn = '<button class="btn pri" id="run">' + (rep ? '↻ Re-run analysis' : '✦ Run citation analysis') + '</button>';
    var head = '<header class="rv"><div style="flex:1;min-width:260px"><h1>Citation Optimizer</h1>'
      + '<div class="sub">What your top-cited <b>included</b> papers are actually cited for — so your protocol and paper cite the field the way the field cites itself.</div></div>' + runBtn + '</header>';

    if (S.err) head += '<div class="banner warn">' + esc(S.err) + '</div>';

    if (!rep) { shell(head + '<div class="empty"><h2>No analysis yet</h2><p>Run it once and Publify will pull the citation sentences for your 10 most-cited included papers, classify what each is cited for, and write a citation strategy.</p></div>'); wire(); return; }

    // stats + legend
    var st = rep.stats || {}, tot = rep.intent_totals || {};
    var totSum = INTENTS.reduce(function (a, k) { return a + (tot[k] || 0); }, 0);
    var statsHtml = '<div class="stats">'
      + statBox('Papers analysed', st.resolved != null ? st.resolved : (S.insights.length), 'of top ' + (st.papers || S.insights.length) + ' included')
      + statBox('Citation sentences', (st.contexts != null ? st.contexts : '—'), 'contexts read')
      + statBox('Influential', (st.influential != null ? st.influential : '—'), 'core citations')
      + statBox('Most common intent', topIntent(tot), totSum ? 'across all citations' : '')
      + '</div>';
    var legend = '<div class="legend">'
      + INTENTS.map(function (k) { return '<span class="chip ' + k + '">' + INTENT_LBL[k] + '</span>'; }).join('')
      + '<span class="chip influ">Influential</span></div>';

    var strat = rep.strategy ? '<div class="strategy"><div class="lab">✦ Citation strategy for this topic</div><div class="prose">' + mdSafe(rep.strategy) + '</div>'
      + '<div class="acts"><button class="btn pri" id="useProt">→ Use in Protocol</button><button class="btn" id="useWrite">→ Use in Journal draft</button><button class="btn" id="exp">⭳ Export brief</button></div></div>' : '';

    var cards = (S.insights || []).slice().sort(function (a, b) { return (a.rank || 0) - (b.rank || 0); }).map(card).join('');
    shell(head + strat + statsHtml + legend + '<div class="listhd">Top papers — ranked by citations</div>' + (cards || '<div class="empty"><p>No paper insights were produced.</p></div>'));
    wire();
  }
  function statBox(k, v, d) { return '<div class="stat"><div class="k">' + esc(k) + '</div><div class="v num">' + esc(v) + '</div><div class="d">' + esc(d || '') + '</div></div>'; }
  function topIntent(tot) { var best = '', n = -1; INTENTS.forEach(function (k) { if ((tot && tot[k] || 0) > n) { n = tot[k] || 0; best = k; } }); return n > 0 ? INTENT_LBL[best] : '—'; }
  function wire() {
    var rb = document.getElementById('run'); if (rb) rb.onclick = runAnalysis;
    var ex = document.getElementById('exp'); if (ex) ex.onclick = function () { downloadText('citation-strategy.md', buildBrief('writing')); };
    var up = document.getElementById('useProt'); if (up) up.onclick = function () { planUse('protocol'); };
    var uw = document.getElementById('useWrite'); if (uw) uw.onclick = function () { planUse('writing'); };
  }

  // ---- boot ----
  addEventListener('keydown', function (e) { if (e.key === 'Escape') closeModal(); });
  if (!BE || !BE.sb) { root.innerHTML = '<div class="center"><div class="box"><h1>Backend unavailable</h1></div></div>'; return; }
  if (BE.mode !== 'cloud' || !BE.user) { root.innerHTML = '<div class="center"><div class="box"><div class="mk"><i></i></div><h1>Sign in</h1><p>Open the Citation Optimizer from a project.</p><a class="btn" href="Landing.html">Sign in</a></div></div>'; return; }
  if (!projId()) { root.innerHTML = '<div class="center"><div class="box"><h1>No project</h1><p>Open the Citation Optimizer from Research → a project → Literature.</p><a class="btn" href="Research.html">← Research</a></div></div>'; return; }
  shell('<div class="empty"><p>Loading…</p></div>');
  load().catch(function () { root.innerHTML = '<div class="center"><div class="box"><h1>Could not load</h1><p>This project may not exist or you may not have access.</p><a class="btn" href="Research.html">← Research</a></div></div>'; });
})();
